#!/bin/nash

# If else expressions

func test(input: integer) {
  if input == 1 {
    out("one");
  } else if input == 2 {
    out("two");
  } else {
    out("idk");
  };
}

for index in [1, 2, 3] {
  test(index);
};
