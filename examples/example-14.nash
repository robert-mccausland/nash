#!/bin/nash

# Recursion

func recurse(index: integer) {
  {
    var index = index.fmt();
    out("iteration ${index}");
  };
  index = index + 1;
  if index < 5 {
    recurse(index);
  };
}

recurse(0);
